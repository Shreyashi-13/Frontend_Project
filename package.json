{
  "name": "obrix-static-demo",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "description": "HTML, CSS and vanilla JavaScript frontend. No build or runtime packages. Node is used only for optional automated tests.",
  "scripts": {
    "test": "node --test tests/*.test.js"
  }
}
